#include <stdio.h>
int gcd(int x,int y)
{
      if(x==0)
    return x;
    else if(y==0)
    return y;
    else if(x>y)
    return(x-y,y);
    return(x,y-x);
}
void lcm(int x,int y)
{
   //this is the code for lcm of two numbers!

}
void main()